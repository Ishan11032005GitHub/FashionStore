# Backend (AutoTriage Generated)

Prompt:
Add a backend in MERN STACK.

## Run locally

```bash
cd backend
npm install
npm start
Frontend:
http://localhost:3000

Health:
GET /api/health

Products:
GET /api/products

Cart:
GET /api/cart
POST /api/cart

Orders:
POST /api/order
